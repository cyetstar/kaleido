<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.onelooker.kaleido.mapper.movie.MovieActorMapper">
    <select id="listByMovieIdAndJs" resultType="cc.onelooker.kaleido.entity.movie.MovieActorDO">
        select t.*
          from movie_actor t
     left join movie_actor_link t1 on t.id = t1.actor_id
         where t1.movie_id = #{movieId}
           and t1.js = #{js}
    </select>
</mapper>
